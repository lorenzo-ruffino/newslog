services:
  newslog:
    image: ghcr.io/lorenzo-ruffino/newslog:latest
    container_name: newslog
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
